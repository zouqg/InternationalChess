package sample;

import javafx.application.Application;
import javafx.scene.control.ChoiceDialog;
import javafx.scene.layout.Pane;
import javafx.stage.Stage;
import sample.entity.chessPiece;
import sample.entity.chessType;
import sample.entity.strategy.QueenStrategy;
import sample.entity.strategy.RookStrategy;

import java.util.ArrayList;
import java.util.Collection;
import java.util.Optional;


public class test extends Application {
    @Override
    public void start(Stage primaryStage) throws Exception {
 /*       Image image = new Image("file:D:\\Java\\作业\\国际象棋\\images\\blackBishop.png");
        ImageView imageView = new ImageView(image);
        Pane pane = new Pane();
        pane.getChildren().add(imageView);
        imageView.setImage(null);


        Scene scene = new Scene(pane,600,600);
        primaryStage.setScene(scene);
        primaryStage.show();*/


        Pane pane = new Pane();

    }
}
